Blog = angular.module("Blog")

Blog.factory('Article', ($resource) -> 
  $resource('<%= Rails.application.routes.url_helpers.api_v1_article_path(":id") %>', { id: '@id'}, {
    'update': {method:'PUT'}
  })

)