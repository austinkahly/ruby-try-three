Blog = angular.module("Blog")

Blog.controller('CommentsController', ($scope, $routeParams, $resource, Comment) ->
  if $routeParams.id
    Article = $resource('/articles/:articleId', articleId: '@id')
    $scope.comments = Article.query()

  $scope.createComment = () ->
    $scope.comments.$save()

)

