Blog = angular.module("Blog")

Blog.controller('CommentsController', ($scope, $routeParams, Comment) ->
  if $routeParams.id
    $scope.comments = Comment.query()

  $scope.createComment = () ->
    $scope.comments.$save()

)

